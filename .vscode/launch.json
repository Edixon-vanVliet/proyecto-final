{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "C Launch",
      "type": "cppdbg",
      "preLaunchTask": "build",
      "request": "launch",
      "program": "${workspaceFolder}\\main.exe",
      "environment": [{ "name": "config", "value": "Debug" }],
      "cwd": "${workspaceFolder}"
    }
  ]
}
